`

if (typeof session.counter == 'undefined') {
  session.counter = 0;
} else {
  session.counter++;
}

`
<h1>#{res.engine.className} engine</h1>

<p><code>session.hello = #{session.counter}</code></p>
